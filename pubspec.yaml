name: catatin
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.4.1
  path: ^1.9.0
  shared_preferences: ^2.3.4
  intl: ^0.19.0
  sqflite_common_ffi: ^2.3.4
    # Only used for desktop platforms

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  
  # Assets
  assets:
    - assets/danankmobile.jpeg
    - assets/gorokmobile.jpeg
    - assets/flutterpng.png
    - assets/dartpng.png
    - assets/sqlitepng.png
